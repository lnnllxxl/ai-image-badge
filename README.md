# AI IMAGE BADGE

Chromeで表示中の画像を確認し、画像来歴情報、端末内画像モデル、任意のOpenAI Content Provenance APIを使って判定理由付きバッジを表示する一般配布版Chrome拡張機能です。

バッジ表示：

- AI【SynthID】
- AI【C2PA】
- AI【モデル判定】
- AIかも／AIかも【C2PA】
- 非生成かも

通常のモデル判定、C2PA・メタデータ解析、周波数・ノイズ解析は端末内で実行します。OpenAIによるSynthID検証は任意・既定オフで、利用者自身のAPIキーを設定した場合だけ画像をOpenAI APIへ送信します。

## インストール

1. GitHub Releasesから `ai-image-badge-release-v0.12.0.zip` を取得して展開します。
2. Chromeで `chrome://extensions` を開きます。
3. 「デベロッパーモード」を有効にします。
4. 「パッケージ化されていない拡張機能を読み込む」から展開フォルダーを選択します。

Chrome内部ページ、Chromeウェブストア、新しいタブなど、Chromeが拡張スクリプトを許可しないページでは動作しません。

## 開発時の確認

```powershell
npm install
npm run check
```

3種類のONNXモデルはGit LFS対象です。clone前に `git lfs install`、clone後に `git lfs pull` を実行してください。Git LFSを使わずに取得すると、モデルが小さなポインターファイルのままになり、ローカル判定は動作しません。一般利用者向けのRelease ZIPにはモデル実体を同梱しています。

## 判定上の注意

本拡張機能の結果は参考情報であり、生成AI由来を確定するものではありません。編集、再圧縮、スクリーンショット、未知の生成器などにより、誤判定や見逃しが発生します。配信元ホスト名やURL文字列だけではAI判定バッジを表示しません。判定結果を画像の真贋、著作権、制作者や掲載者への評価、その他の最終判断に使用しないでください。

## 開発支援ツールについて

本拡張機能のコードは、OpenAIのChatGPTを利用して作成しています。また、AnthropicのClaudeを利用してコードのチェックを行っています。コードの採用、修正、動作確認および配布に関する最終的な判断と責任は公開者が負います。

本拡張機能は、OpenAI、ChatGPT、AnthropicまたはClaudeの公式製品、提携製品、後援製品もしくは認定製品ではありません。

## セキュリティ監査レポート

[Claudeによるセキュリティ監査レポート](SECURITY_AUDIT_claude.md)を公開しています。対象は **v0.12.0の一般配布版** です。提供されたレポートを本文の変更なしで掲載しています。

この監査レポートはv0.12.0の公開ソースを対象とする静的レビューです。実行時の動的解析、依存パッケージを含むサプライチェーン全体、およびv0.12.0以降の変更は監査対象に含まれません。

レポートの範囲はソースコードの静的レビューと、同梱バイナリのファイルヘッダーによる形式検証です。実行時の動的解析や、依存パッケージの改ざん有無を含むサプライチェーン全体の検証は対象外です。

このレポートはAIによるレビュー結果であり、独立したセキュリティ専門家による監査や、脆弱性が存在しないことの保証ではありません。OpenAI連携時の画像送信やAPIキーの保存に関する注意事項もレポート内で確認できます。

## ライセンス

本プロジェクトは [MIT License](LICENSE) で公開します。同梱する第三者コンポーネントの条件は `THIRD_PARTY_NOTICES.md` と `third_party/` を確認してください。

セキュリティ上の問題は、公開Issueではなく [`SECURITY.md`](SECURITY.md) の手順に従って非公開で報告してください。

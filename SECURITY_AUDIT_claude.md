# セキュリティ監査レポート — AI IMAGE BADGE

- **対象**: AI IMAGE BADGE（Chrome拡張機能, Manifest V3）release版
- **監査バージョン**: v0.12.0
- **監査方法**: 全ソースファイルの静的レビュー（手動読解＋パターン検索）。同梱バイナリ（`.onnx` / `.wasm`）はファイルヘッダーによる形式検証を実施。
- **監査観点**: 外部への不審なデータ送信／シェル・子プロセス実行／eval等の動的コード実行／難読化／機密情報アクセス／永続化／破壊的操作／危険な依存関係／インジェクション／目的外処理

## 総合結論

**重大な脆弱性・マルウェア的挙動は検出されませんでした。** 総合的な危険度は **低** です。

- 開発者独自のサーバーへ画像や閲覧情報を送信する処理は存在しません。
- ページ上の画像判定（メタデータ解析・ローカルAIモデル推論・周波数解析・ページ文脈解析）はすべて端末内で完結します。
- 唯一の外部通信は、ユーザーが任意で有効化する **OpenAI公式 Content Provenance API** 連携のみで、送信にはユーザー自身が入力したAPIキーを使用します（詳細は指摘1）。
- `eval` / `new Function` 等の動的コード実行、シェル・子プロセス実行、難読化、Cookie・パスワード・SSH鍵などの不正取得、永続化機構の悪用、破壊的なファイル操作は確認されませんでした。
- APIキーの保存領域は拡張機能の信頼済みコンテキストのみに限定されており、全Webページに注入されるコンテンツスクリプトからは読み取れない設計になっています（詳細は「権限とデータ保護の設計」参照）。
- 判定結果のポップアップには「本判定は参考情報です。画像の真贋、著作権、制作者や掲載者への評価を確定するものではありません。」という注意書きが常に表示されます。

---

## 指摘事項

### 指摘1（情報提供・ユーザー周知目的）: OpenAI Content Provenance APIへの画像アップロード

- **該当ファイル**: `openai-provenance.js`、`background.js`（`analyzeOpenAiProvenance` / `fetchImageForOpenAi`）
- **概要**: 設定で「OpenAI SynthID検証を使う」を有効にした場合のみ、表示中の画像ファイルが `POST https://api.openai.com/v1/content_provenance_checks` へ送信されます。送信先はOpenAIの公式ドメインで、開発者独自のサーバーは経由しません。
  - この機能は**既定で無効**です。
  - 送信にはユーザーが `options` 画面で入力した自分自身のAPIキーを使用します（`chrome.storage.local` に保存、Chrome同期の対象外）。
  - 画像取得時は閲覧セッションのCookieや認証済みキャッシュを使用しません（`credentials: "omit"`, `cache: "no-store"`）。ログインが必要な非公開画像は取得できず、OpenAIへは送信されません。
  - ページURL・alt説明文・閲覧履歴はOpenAI APIへ送信されません。
  - 拡張機能に同梱のテストページ（`test/test.html`）にある「SynthID」デモ画像はクリック操作の確認専用の模擬表示で、実際にOpenAI APIを呼び出すことはありません。
- **評価**: 脆弱性ではなく、開示された仕様どおりの挙動です。ただし「画像ファイルが外部（OpenAI）へ送信されうる」こと自体は利用者が把握しておくべき重要な情報のため、本レポートに明記します。
- **利用上の注意（拡張機能の設定画面にも同様の案内があります）**:
  - この拡張専用のOpenAI APIキー・プロジェクトを作成し、利用上限を低めに設定することを推奨します。
  - 拡張機能のストレージは端末上で暗号化されません。APIキーを設定した状態のChromeプロフィールや拡張機能ファイル一式を、信頼できない端末や第三者と共有しないでください。

---

## 権限とデータ保護の設計

| 権限 / 設定 | 用途 | 評価 |
|---|---|---|
| `scripting` | 拡張読み込み前から開いていたタブへコンテンツスクリプトを注入 | 注入対象は拡張機能自身のmanifestに宣言済みのファイルのみ（`chrome.runtime.getManifest().content_scripts`から動的に取得）。リモートコードの注入なし |
| `storage` | 設定・APIキーの保存 | 必要最小限 |
| `offscreen` | 全タブで1つのローカルAI画像モデルを共有実行するためのオフスクリーンドキュメント | 端末内処理専用、外部送信なし |
| `host_permissions`（`http://*/*`, `https://*/*`） | 全ページで画像バッジ表示・画像先頭部分の端末内解析を行うため | 機能上必要な範囲。取得した画像データが本体を離れるのはOpenAI連携を有効化した場合のみ（指摘1） |
| `chrome.storage.local` の `TRUSTED_CONTEXTS` 制限 | APIキーを含むローカル保存領域を、拡張機能自身のページとService Workerのみに限定し、全Webページに注入されるコンテンツスクリプトからのアクセスを遮断 | 多層防御として有効。コンテンツスクリプト側にAPIキーを扱うコードは存在しないが、実行環境レベルでも読み取りを禁止している |

コンテンツスクリプト（`content/content.js` ほか）は各ページの独立した実行コンテキスト（ISOLATED world）で動作しており、訪問先のWebページ自身のJavaScriptから拡張機能の内部状態を書き換えることはできません。また、`activeTab`権限は要求しておらず、要求する権限は上記4項目に限定されています。

---

## カテゴリ別チェック結果

| # | 観点 | 結果 |
|---|---|---|
| 1 | 外部への不審なデータ送信 | 開発者サーバーへの送信なし。外部送信はOpenAI公式APIのみで、指摘1のとおりオプトイン・開示済み |
| 2 | シェル・PowerShell・子プロセス実行 | 該当なし |
| 3 | eval・動的コード実行 | 検出なし。CSPの`'wasm-unsafe-eval'`は、同梱のONNX Runtime（WebAssembly）を動作させるための標準的な設定であり、`script-src 'self'`と併用されているため外部スクリプトの実行は許可されていない |
| 4 | Base64・文字列連結による難読化 | 検出なし。全ファイルが可読なソースコードで構成されている |
| 5 | Cookie・パスワード・APIキー・SSH鍵へのアクセス | `chrome.cookies`権限は未使用。OpenAI APIキーはユーザー自身の入力を`chrome.storage.local`（`TRUSTED_CONTEXTS`）へ保存するのみで、ハードコードされた鍵・トークンは検出されず |
| 6 | スタートアップ・レジストリ・cron等への永続化 | 該当なし。使用するストレージは`chrome.storage`（設定保存用）のみで、Manifest V3の想定範囲内 |
| 7 | 不要なファイル削除・権限変更 | 該当なし |
| 8 | 危険な依存パッケージ・インストールスクリプト | 該当なし。依存パッケージは`onnxruntime-web`（Microsoft公式）のみ。同梱の`.onnx`/`.wasm`ファイルはヘッダー検証により正規フォーマットであることを確認済み |
| 9 | SQLインジェクション・XSS・コマンドインジェクション | 該当なし。バッジ詳細のUIは`textContent`/`createElement`によるDOM構築で、HTML文字列の組み立てを行っていない（`innerHTML`は不使用） |
| 10 | プログラムの目的と一致しない処理 | 該当なし。実装内容（メタデータ解析、ローカルAIモデル推論、周波数解析、ページ文脈解析、任意のOpenAI公式来歴検証、バッジ表示、同梱テストページでの動作確認）は説明文と一致している |

---

## まとめ

現バージョンにおいて、修正を要する脆弱性は確認されませんでした。利用者に把握しておいていただきたいのは指摘1（OpenAI連携を有効化した場合の画像送信）のみであり、これは既定で無効かつ利用者自身の判断で有効化する機能です。APIキーの保存領域を信頼済みコンテキストに限定する、判定結果に免責文言を明示するなど、データ保護・利用者への説明責任の両面で妥当な設計となっています。

*本レポートは公開されたソースコードの静的レビューに基づくものであり、実行時の動的解析やサプライチェーン全体の検証（依存パッケージの改ざん有無等）は範囲外です。*
